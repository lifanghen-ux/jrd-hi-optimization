# 代码审计

审计对象：活动 JRD_Validation_N6 工程；日期：2026-09-09。

| 项目 | 结论 |
|---|---|
| 五个 Direct fitness | 数学一致；JADE 少一个未用 LCM 参数 |
| 五个 Indirect fitness | 数学一致；算法内部各有一份 fitness |
| m=n 与空组 | n 是最大标签数；空组跳过，旧编码仍保留对应频率变量 |
| Direct penalty | 正上三角同组 pair，各一次；无 LCM |
| Indirect penalty | 相同正上三角除以 LCM(Ki,Kj)，再除以 T |
| 整数边界 | 默认 K,F=1..20，入口设置，不是四规模 MAT 字段 |
| 旧批处理命名 | 上轮修复 TC_Ind/TC_Dir 与保存变量不一致 |
| 历史全算法 MAT | 成本全零，无法从其中恢复真实成本 |
| 历史 Excel/PNG | 静态数值有对应关系，缺少完整原始来源 |
| seed 与重复 | 上轮旧 runner 已支持；本轮新求解器也支持并保存 |
| 历史结果含义 | 主要为单次运行，不是 30-run 分布 |
| Direct 旧解码 | bestK 是组号、bestT=0；新记录保存真实组周期 |
| DE 交叉/选择 | 与 JADE 及标准逐目标竞争存在差异，未混改历史算法 |
| 解码边界 | 旧 X=1 有越界风险；新编码无此路径，旧包装层拒绝非法解 |
| Toolbox | 基础 MATLAB；哈希依赖自带 JVM |
| N10 最优值 | 在测试中独立 DP 求得，不由新启发式特判产生 |

此前仅有存储修复，没有 Direct_V1 和算法优化实现。
本轮补齐精确组评价、集合邻域、multi-start、ILS、MATLAB DP，
并新增 Indirect 的精确内层和整数外层搜索。

## 当前限制

- 有限时间外层仍是启发式，大规模没有全局最优证书。
- Split 检查双商品、penalty 引导及有限随机二分，不穷举全部子集。
- 缓存满时清空继续，会影响效率，不影响成本正确性。
- 时间预算在评价边界检查，不是硬实时终止。
- 等时间结果随负载可能变化；完全重现应使用有限搜索配置与无时间约束。
- 两侧评价内核工作量不同，不能直接当作等评估预算。
- 四规模各一个固定实例，seed 重复不替代跨实例研究。
- 检查点已实现，自动断点续跑未实现。
- 新 runner 比较新 Direct/Indirect；旧算法仍使用独立代数/种群预算 runner，
  没有宣称新旧算法已经完成等时间排名。
- 审计完成时未新建远程仓库、上传文件或运行正式实验；后续用户已授权私有仓库上传，正式实验仍未运行。
