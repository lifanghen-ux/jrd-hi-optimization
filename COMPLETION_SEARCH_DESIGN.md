# 完整搜索任务驱动的策略比较

核心原则：尽可能控制求解器、随机性与计算预算影响。时间只记录，不参与策略优劣或主终止判断。统一搜索层级，保留变量结构差异，不声称求解器性能完全相同。

## 新旧入口

- 新入口 `Run_Strategy_Experiment`，输出 `Results_Completion`，schema 2。
- 原 `Run_Optimized_Experiment` 和两侧 `MS_VND_*` 保持不变，仅用于旧等时间结果复现。不要用旧入口启动本轮实验。
- 新共用外层 `JRDCompleteSearch` 调用原模型内层和策略专属 VND。不改数据、成本、频率/整数边界或旧结果。

## 统一层级与策略差异

每个起点执行完整局部搜索，再执行配置的有效扰动及完整局部搜索。结束整个任务需要 Starts 个起点和 Starts×ILSIterations 次有效扰动全部完成。局部搜索没有时间或评价次数上限。时间仅诊断。

直接侧保留移动、合组、交换、两商品拆组和 penalty 引导拆组；本入口关闭额外随机拆组抽样，使局部搜索在指定邻域上可重复。并不穷举所有拆组，不能称全邻域全局最优。间接侧保留逐坐标全 K 候选扫描及 K 交换。内层均按原实现精确计算固定外层方案的频率/周期。

间接侧现在还可配置两类双坐标块 K 邻域：与 Penalty 无关的固定循环配对，
以及按原有正 Penalty 边权重排序的冲突配对。块搜索的独立消融必须先于正式
Direct/Indirect 比较；最终入口将所选 profile、块宽度及候选数写入配置和哈希快照。
不得通过修改 Penalty 数据来替代求解能力验证。

起点第一个为集中结构，第二个为分散结构，之后随机生成。直接侧随机选择组数并保证非空覆盖，不再单侧使用贪心起点。间接侧在有界整数 K 域内随机。重复起点尝试重采样 32 次后如仍重复则记录，不无限循环；有限状态域不可能保证任意多个唯一方案。

两侧按同一商品比例设扰动强度，但操作不同：直接侧移动商品，间接侧修改不同坐标到不同 K。直接侧多步移动若相互抵消，用单次有效移动兜底。有效扰动只保证决策改变，不保证局部搜索后落入不同吸引域。

扰动规模为 `min(N,max(3,ceil(PerturbFraction*N)))`，避免小规模按比例取整后只剩单商品微扰，难以离开已完成的单商品邻域局部最优。直接侧是移动次数，间接侧是不同坐标数，不把二者称为相同距离。

使用相同 restart 概率与接受容差。每个逻辑起点/扰动单独重置随机流，防止一侧分支数量改变后续随机序列。同规模、种子、层级保持配对；不能把相同种子解释为相同搜索覆盖率。不同层级复用种子有利于比较，但不保证单次更大配置必然更好，所以累计保留历史最好解。

## 分级验收

默认起点数 `[4 8 16]`，每起点扰动轮数 `[10 20 40]`。完整执行每一层全部规模、两侧及种子后，检查双方累计最低成本连续 2 个层级相对改进不超过 1e-6，并要求本层各自种子成本极差/最低成本不超过 1e-3。所有规模两侧一起达标才报 `search_stable`。

这些阈值是可配置的研究停止规则，不是全局证书。所有配置层级完成仍未达标，状态为 `schedule_exhausted_not_stable`，必须如实报告，允许追加层级。不得把该状态解释为已充分搜索。默认配置不是已验证足够的搜索量。

正式层级必须增加起点数或扰动轮数，不能重复完全相同配置来凑稳定次数。如果已知某侧精确最优值，启发式还必须达到该值的相对容差，才允许报 search_stable。精确答案不注入搜索，稳定落在已知非最优解会被拒绝验收。

## 数学界限

共同下界：去掉正的主订货成本及非负 penalty，利用 Cauchy-Schwarz 得到

`LB = sum_i min_f sqrt(2*(sw_i+f*sr_i)*D_i*(hw_i+(hr_i-hw_i)/f))`。

对 Direct：组成本中 A、B 分别为对应单项之和加非负成本，`sqrt(sum A_i * sum B_i) >= sum sqrt(A_i B_i)`。对 Indirect：A 项除以 K、B 项乘以 K，单项乘积抵消 K。同样成立。下界可能很松，不能保证识别胜负。

N<=10 自动计算 Direct DP 最优值；完整 K 域状态数<=100000 才尝试 Indirect 全枚举并配合精确内层。状态数只决定是否调用精确算法，不截断已开始的精确搜索。不把精确答案注入启发式。

两侧最好可行成本 UB 与下界 LB 一起保存。`(UB-LB)/UB` 是相对界限差距，不是已知真实误差。只有 UB_D < LB_I 或 UB_I < LB_D（含数值裕量），或两侧精确证书，才给出最优成本顺序结论；其余只比较当前最好可行成本。

## 验证与运行

在 MATLAB 工程目录：

```matlab
Setup_JRD;
Test_JRD_All;
Test_JRD_Completion;
```

正式入口同时检查两套测试的源码/数据哈希绑定。改动源码或数据后需要重新运行。先做短任务量验证（不是按秒验证）：

```matlab
[check,checkDir] = Run_Strategy_Experiment('Phase','validation', ...
    'NList',10,'Runs',2,'StartsSchedule',[2 2], ...
    'PerturbSchedule',[1 1],'StableLevels',1);
```

正式研究需手动启动，不会由测试启动：

```matlab
[Study,out] = Run_Strategy_Experiment('Phase','formal', ...
    'NList',[10 30 50 100],'Runs',30, ...
    'StartsSchedule',[4 8 16],'PerturbSchedule',[10 20 40]);
```

默认全配置为每侧每种子 868 次完整局部搜索，四规模三十种子两侧共 208320 次。可能耗时很长，目前没有可信总耗时估计。不要据旧四小时结果预测新方案的完成时间。

## 进度与续跑

每次完整局部搜索结束打印起点、扰动轮次、成本，并保存 `tasks/*.mat`。主 `study.mat` 在每个求解任务完成后保存；正在执行的局部搜索内部不存盘，中断后会重做最后一个未完成的局部搜索。

检查点含输入、源码清单、选项、当前解、最好解及逻辑步骤。每个动作重新设置种子，续跑可重现决策路径；缓存不保存，重建会改变耗时和缓存计数，不影响成本排名。写入先保存同目录临时文件，再替换目标，保留 `.previous` 上一检查点以供恢复。不自动删除旧文件。

```matlab
% 使用与原运行一致的配置；替换 actualRunDirectory
[Study,out] = Run_Strategy_Experiment('Phase','formal', ...
    'NList',[10 30 50 100],'Runs',30, ...
    'StartsSchedule',[4 8 16],'PerturbSchedule',[10 20 40], ...
    'ResumeDir','actualRunDirectory');
disp(Study.Comparison);
```

允许仅在原层级序列后追加新层级，例如起点 `[4 8 16 32]`、扰动 `[10 20 40 80]`；复用既有结果。其他配置或源码/数据不一致拒绝续跑。

`study.mat` 变量名为 `payload`，用 `x=load(path,'payload'); Study=x.payload;` 读取。`summary.csv` 每层输出最低、平均、最差、累计最低、种子极差、稳定层数及下界。`comparison.csv` 比较两侧累计最低可行成本，节约率为正代表 Direct 更低；结论列区分最优性证据与未证明排序。新旧 CSV 口径不同，不混合汇总。

## 限制

四个规模仍各一个固定实例，不代表总体独立实例胜率。完整多起点任务、成本稳定和控制随机性都不能证明求解器同等强，也不能排除其他算法找到更好解。当前保留模型边界 K/F=1..20；结果不代表无界模型。
