# JRD-HI 实现与验收汇报

日期：2026-09-09。
工程阶段：核心求解器和本地验收完成；用户已批准创建私有仓库并上传，尚未运行正式实验。

## 1. 检索结论

本轮开始时，工程只有十个旧搜索算法，以及上一轮的命名/保存修复。
精确组评价、规范 partition、集合邻域、multi-start、ILS 和 MATLAB ExactDP 均未实现。
现在这些核心模块已经实现并通过 MATLAB R2024a 验收。

“实现完备”指核心算法、计算正确性和运行管线具备启动条件，
不表示 N=30/50/100 已有全局最优证明，也不表示猜想已得到正式实验支持。

## 2. 已落实的优化

| 功能 | 实现及意义 |
|---|---|
| 固定组精确优化 | GroupCostExact 枚举合法 F，解析 T，外层不再随机搜索频率 |
| 规范标签 | CanonicalizePartition 消除纯标签等价，不再用连续数 floor 产生组号 |
| 完整解 | PartitionCost 返回每组商品、F、T、组成本与总成本 |
| 多种初始方案 | 全部合并、全部单独、固定顺序贪心、随机顺序随机贪心 |
| 移动 | 商品移往已有组或新组 |
| 合并 | 两组完整合并并精确重选组内 F/T |
| 交换 | 跨组交换两个商品 |
| 真正拆分 | 双商品拆出、penalty 引导二分、随机二分；不把单商品移动重复算作新增拆分能力 |
| VND | 各邻域 best-improvement，改善后回到第一个邻域 |
| ILS | 多商品扰动后再 VND；探索解可暂时更差，单独保存历史最好解 |
| 有效扰动 | 排除原组和单商品独立组改名这种无效动作 |
| 组成本缓存 | 每次求解独立缓存，绑定数据/边界，避免跨实例污染；设置容量上限 |
| 增量评价 | 每个邻域候选只重算受影响组 |
| 小规模精确解 | ExactDP_Direct 穷举非空子集并做划分 DP，仅用于 N<=16 |
| Indirect 精确内层 | 固定 K 后，遍历所有 F 切换阈值区间，精确联合优化 T/F |
| Indirect 整数外层 | 单坐标完整整数取值、双商品 K 交换、多起点、多坐标扰动与 VND |
| LCM 预处理 | 预存非零 pair 与 LCM 表，减少 Indirect 内层重复循环 |
| 预算与状态 | 分别记录搜索/总耗时、起点开始/完成数、已接受动作、候选次数、缓存计数 |
| 安全停止 | 预算中止仍返回已有完整可行解；初始可行解允许一次必需评价 |
| 可重现 | 保存 seed 和配置；求解器恢复调用方 RNG 状态 |
| 等时间新实验 | 新 Direct/Indirect 使用相同搜索时间，并交替策略执行顺序 |
| 追溯 | 保存输入和源码快照、SHA-256、完整解、收敛轨迹和原始任务记录 |
| 正式入口验证 | formal 模式要求通过验收且代码/数据清单未改变，并要求至少 30 个 seed |

新算法保持原成本数学定义，正上三角 penalty 的处理与旧源码一致。
十个旧搜索算法保持原样，用作历史回归和基线。

## 3. 数学正确性

运行 Test_JRD_All，最终完成于 2026-09-09 15:34:35，
MATLAB 24.1.0.2537033 (R2024a)，状态 PASS。

| 检查 | 结果 |
|---|---|
| 新 fixed-F Direct 对五套旧 fitness | 最大相对误差 4.2772e-16 |
| 新 fixed-K/F Indirect 对五套旧 fitness | 最大相对误差 3.2249e-16 |
| 解析 T 对稳定等价目标 fminbnd | 最大绝对误差 4.8251e-09，小于 1e-8 |
| Indirect 精确内层对全 F 穷举 | 最大相对误差 1.8551e-16 |
| hr=hw、hr<hw、FInf>1 | 全频率穷举交叉验证通过 |
| N=6 全划分独立枚举 | 203 个 partition 与 DP 一致 |
| 标签不变性、频率精确优化不劣 | 通过 |
| 缓存顺序不变性、容量、数据隔离 | 通过 |
| 非法分组、重复/遗漏商品、非法 K | 拒绝非法输入 |

数值测试中的一个改进：
直接对 A/T+BT/2 运行 fminbnd 时，平坦最低点附近最大周期误差为 1.5229e-08。
改用等价目标 (sqrt(A/T)-sqrt(BT/2))^2；
它只从原目标减去常数 sqrt(2AB)，最优位置不变，降低最低点附近的数值精度损失。
验收阈值保持 1e-8，没有修改求解器的成本公式。

## 4. N=10 精确基准和 20-seed 验收

ExactDP_Direct 独立得到：

    TC*=12059.2516110867073
    G1={1,7,8,10},       f1=2, T1=0.325500258832
    G2={2,3,4,5,6,9},    f2=2, T2=0.189758234593

MS_VND_Direct 不调用 DP，也没有特殊 N=10 路径或目标值硬编码。
测试中的预期常数仅用于断言。

20 个 seed 为 20261001..20261020。
验收配置：8 个起点，每个起点最多 5 次 ILS，SplitTrials=3，
MaxEvaluations=1023、MaxSeconds=Inf，CacheLimit 默认 50000。
1023 是此规模全部非空子集数；这项固定配置是测试预算，不是 solver 特判。

| 指标 | 结果 |
|---|---|
| 命中精确最优 | 20/20 |
| 最好 gap | 0% |
| 中位 gap | 0% |
| 最差 gap | 0% |
| 单次耗时最小/中位/最大 | 0.963 / 1.057 / 1.361 秒 |

之前使用的缩减验收配置（4 起点、每起点 1 次 ILS）能找到全局最优，
但未通过预定稳定性门槛。没有据此降低门槛；加强有效扰动并增加搜索后通过。
因此不能把这些结果解读为任何极短时间预算下都必然达到全局最优。

## 5. 运行和保存验证

- Direct/Indirect 固定 seed、固定步骤/评价预算的两次重跑一致。
- 两侧单评价预算都能返回完整可行解。
- 四邻域在单组和多组方案上，增量成本与完整复算一致，接受解不变差。
- N=10/30/50/100 均通过 0.25 秒预算短运行和可行性检查。
- 新管线通过两 seed、两策略的短运行、MAT 重载和 Saving 复算。
- 旧管线再次执行 40 次短调用，保存、重现、图文件和数据保护检查通过。
- 旧十个算法 SHA-256 与原备份一致，七个项目 MAT 的 SHA-256 与原 ZIP 一致。

短管线与四规模 smoke 只检验运行正确性，不作正式策略优劣证据。
独立的有限搜索测试中，Indirect N10 得到 12587.5415880655；
它只用于可行性/重现测试，尚未超过已知历史 Indirect 记录，不能称最优。
新的 Indirect 内层虽然精确，外层仍需要正式预算和多起点实验检验。

最终 readiness.mat 绑定 64 个条目：59 个活动 MATLAB 文件和 5 个固定数据。
完整机器可读结果位于 Validation_V1：
mathematics.mat、search.mat、n10_acceptance.mat、storage.mat、readiness.mat。
这些自动生成测试文件不在拟上传清单中；上传后可在 MATLAB 环境重建。

## 6. 仍未完成、也不应混称为已验证的内容

1. N=30/50/100 的 30 次正式结果、BKS 改进和分布尚未测量。
2. 大规模全局最优/有效下界证书尚未实现。
3. 当前每个规模只有一个实例；跨实例胜率和统计检验需另设计独立实例集。
4. 新旧算法的等时间全面排名尚未实现。当前新 runner 只公平计时比较新 Direct/Indirect；
   旧算法仍使用旧代数/种群预算管线。
5. 两种评价内核工作量不同，不能把相同次数作为跨策略公平预算。
6. 自动断点续跑尚未实现，已实现逐任务保存和错误检查点。
7. 协作式时间预算可超出一个评价内核及必要收尾时间。
8. 用户已批准创建/上传私有仓库；正式实验仍需用户手动启动，操作说明见 README。

## 7. 提交计划

详见 REPOSITORY_UPLOAD_PLAN.md。
建议 lifanghen-ux/jrd-hi-optimization，Private。
上传 59 个 MATLAB 文件、五个固定数据和六个说明/配置文件，共 70 个文件。
不包含论文、签名、历史 ZIP、历史结果和自动生成目录。
不会擅自附加开源许可证或移除旧源码署名。
